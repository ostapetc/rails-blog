<article class="page">
  <h1>
    <% if preview %>
        <%= link_to page.title, page %>
    <% else %>
        <%= page.title %>
    <% end %>
  </h1>

  <% if preview %>
      <%= raw page.text.truncate 1000 %>
  <% else %>
      <%= raw page.text %>
  <% end %>

  <p style="margin-top: 5px">
    Tags:
    <% page.tags.each do |tag| %>
        <a href="<%= pages_path(tag_id: tag.id) %>"><span class="label label-info"><%= tag.name %></span></a>
    <% end %>

    | <i class="icon-calendar"></i> <%= page.created_at.strftime('%B %d, %Y') %>
    | <i class="icon-comment"></i> <a href="#">3 Comments</a>
    | <i class="icon-share"></i> <a href="#">39 Shares</a>
  </p>

  <hgroup style="margin-top: 5px">
    <% if preview %>
        <%= link_to 'Read more', page, class: 'btn btn-primary' %>
    <% end %>

    <%= link_to 'Edit', edit_page_path(page), class: 'btn btn-success' %>
  </hgroup>
</article>

