<%= render @page, preview: false %>

<% if @page.comments.present? %>

<% else %>
    <div class="alert">Еще никто не оставлял комментарии</div>
<% end %>

<a name="comments"></a> <br/>
<div id="comments-widget" data-page="<%= @page.id %>" data-form="new_comment"></div>

<%= render partial: 'comments/form' %>
