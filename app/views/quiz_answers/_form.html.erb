<%= form_for(@quiz_answer) do |f| %>
  <% if @quiz_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quiz_answer.errors.count, "error") %> prohibited this quiz_answer from being saved:</h2>

      <ul>
      <% @quiz_answer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label : %><br />
    <%= f.text_field : %>
  </div>
  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_field :text %>
  </div>
  <div class="field">
    <%= f.label :is_right %><br />
    <%= f.check_box :is_right %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
